設計概念
```
底下導覽列分為 首頁，密碼，設定 這三種

首頁：
最近使用

密碼：
已建立密碼卡片

設定：
安全
  啟用 Face ID
  深色模式切換
資料
  匯出備份
  清除所有資料
v1.0 ・ 完全離線 ・ 無雲端同步
```
```
密碼頁面中要有
右上角＋ 可以右從左滑進建立密碼頁面

建立帳號密碼頁面
電子信箱和密碼和加密金鑰跟資料鎖的輸入欄位顯示邏輯：
（輸入格子內最右側要有眼睛圖案預設關閉的狀態，表示隱藏加密的提示)（當被加密時以凱薩加密法＋3 顯示該內容，點擊後輸入狀態時自動開啟眼睛變成明文，修正時是明文當點擊其他地方後就請變回密文。除非使用者自己按下眼睛切換隱藏加密狀態。）

標題
  [ ]（使用者自行輸入欄位）
平台
  [ ]（使用者自行輸入欄位，如果預設選單中沒有的話）（點選後往下出現選單）
電子信箱
  [ ]（使用者自行輸入欄位）
密碼
  [ ]（使用者自行輸入欄位）
  隨機生成密碼
  [密碼長度幾位元][複雜程度]
  [只要純數字]
  [只要全英文]
加密金鑰
  [ ]（使用者自行輸入欄位）
  隨機生成金鑰
  [密碼長度幾位元][複雜程度]
  [符合密碼長度生成金鑰]（使用OTP加密法時需要加密解密使用）
  [只要純數字]
  [只要全英文]
資料鎖
  [ ]（使用者自行輸入欄位）
  隨機生成金鑰
  [密碼長度幾位元][複雜程度]
  [符合密碼長度生成金鑰]（使用OTP加密法時需要加密解密使用）
  [只要純數字]
  [只要全英文]
確認資料鎖
  [ ]（使用者自行輸入欄位）
  隨機生成金鑰
  [密碼長度幾位元][複雜程度]
  [符合密碼長度生成金鑰]（使用OTP加密法時需要加密解密使用）
  [只要純數字]
  [只要全英文]
```
v1.0 ・ 完全離線 ・ 無雲端同步
```
點擊查看明文時輸入的資料顯示一樣和編輯資料時的邏輯一樣，不要直接顯示明文，除非自己切換。
長按平台卡片（所有）時出現的不只是刪除按鈕再加上可以更顯示圖示的功能，因為是很小的iCon而已所以照片圖片上傳後自動壓縮到非常小的檔案大小儲存即可。
加密金鑰 和 資料鎖加上所有字元大寫的選項。
編輯中在標題底下加入副標題的輸入欄位。副標題顯示在已建立卡片的標題底下。
```
